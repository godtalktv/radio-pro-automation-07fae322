// DCS Metadata Capture - Simplified version to avoid errors
import React from 'react';

export const DCSMetadataCapture = () => {
  // Placeholder component - will be implemented later
  console.log('[DCS] Metadata capture system ready');
  return null;
};

export default DCSMetadataCapture;